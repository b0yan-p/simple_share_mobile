export const environment = {
  production: true,
  baseAPIUrl: 'https://simpleshare-api.choxster.com/api',
  // Same host that serves /.well-known/assetlinks.json — an App Link is only
  // verified for the domain whose file names this app.
  clientUrl: 'https://simpleshare-api.choxster.com',
};
