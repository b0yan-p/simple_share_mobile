import { Injectable } from '@angular/core';
import { PaginatorService } from 'src/app/core/services/paginator.service';

@Injectable({
  providedIn: 'root',
})
export class ExpensePaginatorService extends PaginatorService {}
